# Pinned, the way the course pins everything: an exact version, never >=.
# Lesson-specific packages (google-adk, fastmcp, streamlit, google-cloud-discoveryengine, ...) are installed by
# each notebook's first cell, pinned there.

# Vertex AI / Gemini
google-genai==2.21.0
google-cloud-aiplatform==2.1.0
google-cloud-firestore==2.30.0
google-cloud-storage==3.13.1
google-cloud-bigquery==3.45.0
google-cloud-secret-manager==2.30.0
google-cloud-logging==3.16.3

# Used across the notebooks
pydantic==2.13.5
pandas==2.3.2
requests==2.34.2

# Notebook tooling floats on purpose: it never ships in an image
jupyter
ipykernel
ipywidgets
